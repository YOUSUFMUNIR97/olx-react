import { IconButton } from '@mui/material'
import React from 'react'

const SMIcon = (props) => {
  return (
    <>
    <IconButton>
        {props.iconName}
    </IconButton>
    </>
  )
}

export default SMIcon